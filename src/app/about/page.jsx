export const metadata = {
    title: "About",
    description: "About select",
};

const AboutPage = () => {
    return (
        <div>
            <h3>About select</h3>
        </div>
    );
};

export default AboutPage;
